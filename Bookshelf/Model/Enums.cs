namespace Bookshelf.Model
{
    public enum Tris
    {
        Title,
        Year,
        Publisher,
        Authors
    }

    public enum Orders
    {
        Ascending,
        Descending
    }
}